# Simple Stock Agent v1 — 依赖
# 安装: pip install -r requirements.txt

alpaca-py>=0.29.0          # Alpaca 美股数据 + 交易 API
pandas>=2.0.0              # 数据处理 (表格操作)
numpy>=1.24.0              # 数值计算
tabulate>=0.9.0             # 终端表格打印 (好看的输出)
python-dotenv>=1.0.0        # 从 .env 文件读取 API Key
